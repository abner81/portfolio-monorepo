import { Inject, Injectable } from '@nestjs/common';
import { IApplicationService } from '@monorepo/arch/service';

@Injectable()
export class <%= classify(name) %>Service implements IApplicationService {
  constructor(
    @Inject(<%= convertToUpperCase(name) %>_REPOSITORY)
    private readonly repo: I<%= classify(name) %>Repository
  ) {}

  async execute(params: unknown): Promise<void> {
    return;
  }
}
