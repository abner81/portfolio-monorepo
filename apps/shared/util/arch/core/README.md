# shared/util/arch/core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared/util/arch/core` to build the library.

## Running unit tests

Run `nx test shared/util/arch/core` to execute the unit tests via [Jest](https://jestjs.io).
