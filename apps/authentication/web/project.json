{
  "name": "authentication/web",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/authentication/web",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project authentication/web --web",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/authentication/web",
        "root": "apps/authentication/web"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/authentication/web"
        },
        "production": {}
      }
    }
  }
}
